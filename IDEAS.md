# 아이디어 인박스 · Idea Inbox

> 스치는 아이디어를 잡는 그물. 형식 자유, 한 줄이면 충분.
> 실험이 될지 말지는 나중에 판단 — 여기선 일단 잡아두기만 한다.
> (Claude: 대화 중 아이디어가 나오면 즉시 여기에 기록할 것.)

## 잡아둔 것

- [x] ~~**수면 실험실**~~ (#004로 구현 → 소유자 결정으로 폐기) — 잠을 진짜 못 잔다.
      수면 일지: 기록 → 수면 그래프 → 패턴의 의미 분석 → 솔루션 제시.
      현재 갤럭시워치 + 삼성헬스 + Sleep Cycle 사용 중인데, 그것들보다
      **내가 커스텀할 수 있고 쓰기 편한** 것을 원함.
      핵심 통찰: 자동 측정은 워치가 이김 → 이 도구의 승부처는 **주관 레이어**
      (기분·태그·메모)와 **내 방식의 분석·시각화**. 삼성헬스 CSV 내보내기
      가져오기 지원이 킬러 기능 후보.

- [ ] **데이터 → 예쁜 결과물 빌더** (2026-08-20 대화 중) — 뭔가를 "넣으면" 시각적으로
      이쁘게 만들어주는 페이지. 소유자 예시: 이미지 다발 → 토너먼트 대진표,
      수치 → 통계. 확장 후보: 텍스트 → 워드클라우드/문체 리포트, 대화 로그 →
      말버릇·시간대 히트맵, 목록 → 티어리스트/2×2 매트릭스, 날짜 목록 → 타임라인,
      수입·지출 → 생키(돈 흐름), 스탯 → 레이더 비교, 1년치 기록 → 결산 카드(Wrapped),
      이미지 → 색 팔레트/콜라주.
      설계 갈림길: 만능 빌더 하나 vs "붙여넣으면 종류 감지 → 어울리는 출력 추천".
      공통 승부처는 **PNG 저장 = 남는 증거물**.

- [ ] **기기 간 데이터 동기화** (2026-09-01 대화 중) — 회사 PC ↔ 집 PC를 오가다 보니
      localStorage 단독 저장(#003 증거 로그, #008 하루 고리)의 한계가 체감됨.
      유력안: **secret Gist + gist 전용 토큰**으로 저장 시 push / 로드 시 pull
      (정적 사이트 유지, 서버·의존성 0). 데이터가 날짜 키라 병합 쉬움.
      공용 모듈로 만들어 실험들에 끼우는 그림.
      대안(2026-09-04): **구글 로그인 + Drive `appdata`** — 앱 전용 숨김 폴더에 JSON 저장.
      새 기기에서 로그인 한 번이면 끝이라 UX는 우위. 대신 GIS 외부 스크립트 의존,
      1시간 토큰 재발급 처리, Cloud Console 설정(테스트 모드 + 본인 계정)이 필요.
      대안2(2026-09-04): **Google Apps Script 웹앱** — GCP 콘솔 없이 드라이브에 저장하는
      우회로. 소유자 권한으로 실행되는 스크립트를 배포하고 페이지는 그 URL로 fetch.
      토큰 만료 없음, 설정은 배포 버튼 하나. 대신 로그인 UI가 아니라 비밀 키 방식이고
      호출당 0.5~2초로 느림.

## 졸업 (실험이 된 것들)

- [x] 원트 파인더 → #002
- [x] 증거의 벽 → #003
- [x] 수면 실험실 → #004 (설계 중 — 솔루션 DB부터 완성)
