# build output — regenerated by `npm run build`
dist/

# owner profile — private raw results from experiments (#001 profile, #002 wants).
# The digested summary (data/profile.summary.md) is committed; raw data is not.
data/profile.json
data/wants.json

# node / os
node_modules/
.DS_Store
Thumbs.db
