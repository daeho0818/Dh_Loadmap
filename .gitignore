# build output — regenerated by `npm run build`
dist/

# owner profile — private answers from experiment #001.
# The digested summary (data/profile.summary.md) is committed; raw answers are not.
data/profile.json

# node / os
node_modules/
.DS_Store
Thumbs.db
