# 개인 실험실 아카이브 (Personal Lab Archive)

> 관심이 생길 때마다 작은 웹 실험을 하나씩 만들어 번호를 붙여 쌓는 개인 사이트.
> 모토: **"작게, 제대로."** (초기 모토 "주말마다 하나씩 / 완성도보다 완료"는
> 소유자 프로필 데이터에 따라 2026-07-15 폐기 — 주기 압박 없음, 누적 중심)

## 1. 프로젝트 개요

- 정체: `lab.{도메인}` 형태의 정적 사이트. 주말 실험(`#001`, `#002`, ...)이 카드로 쌓이는 아카이브.
- 소유자 프로필: 개발자. 취미 목적. 아이디어가 처음 굴러가는 순간과 결과물이 눈에 보이는 것에서 재미를 느낌. 포트폴리오/기록으로 쌓이는 것이 핵심 동기. 장기적으로 자라는 프로젝트를 원함.
- 레퍼런스: neal.fun 류의 개인 실험 아카이브.

## 2. 목표 / 비목표

### 목표
- 실험 1개 = 독립적인 정적 페이지 1개 (HTML/JS 위주, 프레임워크 강제 없음)
- 인덱스 페이지에 실험 카드가 최신순으로 쌓임
- 활동 잔디(주 단위 히트맵)로 꾸준함이 시각화됨
- 새 실험 추가 비용이 극도로 낮을 것: 폴더 하나 + 메타데이터 파일이면 끝
- GitHub Pages(또는 유사 서비스)로 push 시 자동 배포

### 비목표 (지금은 안 함)
- 댓글, 로그인, 백엔드 서버
- CMS, 데이터베이스
- 완벽한 디자인 시스템 (엔진이 굴러간 뒤 점진 개선)
- SEO 최적화

## 3. 사이트 구조

```
/                  → 인덱스: 헤더, 통계(실험 수/연속 주말/누적), 잔디, 실험 카드 그리드
/experiments/001/  → 실험 #001 페이지 (독립 실행)
/experiments/002/  → ...
/about/            → 소개 (선택)
/feed.xml          → RSS (로드맵 후반)
```

## 4. 저장소(레포) 구조 제안

```
lab/
├── experiments/
│   └── 001-sound-particles/
│       ├── index.html        # 실험 본체 (자체 완결)
│       ├── meta.json         # 카드용 메타데이터
│       └── thumb.svg         # 썸네일 (없으면 빌드가 플레이스홀더 생성)
├── site/
│   ├── build.js              # 핵심: experiments/ 스캔 → 인덱스/잔디 생성
│   └── templates/            # 인덱스 페이지 템플릿, 공용 CSS
├── dist/                     # 빌드 산출물 (배포 대상)
├── .github/workflows/deploy.yml
└── README.md
```

### meta.json 스키마

```json
{
  "id": 1,
  "slug": "sound-particles",
  "title": "사운드 파티클",
  "date": "2026-07-18",
  "week": "2026-W29",
  "tags": ["canvas", "webaudio"],
  "oneliner": "드래그하면 위치에 따라 음높이와 색이 달라지는 오디오 장난감"
}
```

## 5. 기술 스택 (제안 — 확정 전 논의 가능)

- 실험 자체: 바닐라 HTML/CSS/JS 기본. 실험별로 라이브러리 자유 (CDN 허용).
- 빌드: Node.js 스크립트 1개 (`site/build.js`). 프레임워크 없이 시작. 나중에 필요를 느끼면 Astro 등으로 이전 검토.
- 배포: GitHub Pages + GitHub Actions (push → build → deploy).
- 잔디: `meta.json`의 `week` 필드 집계로 빌드 타임에 SVG/HTML 생성. 외부 API 불필요.

## 6. 실험 운영 규칙

1. 한 실험은 주말 하루 안에 끝나는 크기로 자른다. 넘치면 스코프를 줄인다.
2. 미완성이어도 배포한다. "완성도보다 완료."
3. 실험 페이지는 독립 실행 가능해야 한다 (인덱스 없이 URL 직접 접근 OK).
4. 각 실험에 한 줄 소개(`oneliner`)와 태그를 반드시 남긴다. 미래의 회고 재료.
5. 주제 제한 없음: 시뮬레이션, 시각화, 장난감, 데이터 실험 전부 허용.

## 7. 첫 실험 = 소유자 파악 (#001)

**#001 「나를 아는 검사 · Know Thyself」** — 정해진 주제를 이식하는 대신, 첫 실험은
소유자 자신을 파악하는 성향 설문으로 한다(MBTI 검사 같은 형식, 10–30분). 그 결과를
`data/profile.json`으로 저장해, 앞으로 **어떤 실험을 만들지 정하는 1차 참고 자료**로 쓴다.
즉 이 아카이브는 소유자를 학습한 뒤 그에 맞춰 자라난다.

> 특정 실험을 "#001부터 순서대로 이렇게 만들어라"는 사전 지정은 두지 않는다.
> 다음 실험은 #001 결과(build tastes / topics / 자유 서술)에서 도출한다.
> 아이디어 씨앗(시뮬레이션·시각화·오디오 장난감 등)은 백로그로만 느슨히 보유.

## 8. 로드맵

- **v0.1 (첫 주말)**: 레포 생성, build.js(스캔→인덱스+잔디 생성), 실험 #001(나를 아는 검사) 제작, GitHub Pages 배포. → "URL이 존재한다"가 목표.
- **v0.2**: #001 결과 반영 → 소유자 취향에 맞는 실험 1개 신규 제작. 통계/잔디 다듬기.
- **v0.3**: 썸네일 규칙 정리(수동 SVG 또는 자동 플레이스홀더), 실험 1개 추가.
- **v1.0**: about 페이지, RSS, 다크 모드.
- **이후 백로그**: 태그 필터, 랜덤 실험 버튼, 연말 회고 자동 생성 페이지, OG 이미지 자동화.

## 9. Claude Code 작업 가이드

- 첫 세션 목표는 v0.1 완주. 디자인 욕심은 v0.2 이후로 미룬다.
- build.js는 의존성 최소화(가능하면 Node 내장 모듈만)로 시작한다.
- 새 실험은 독립 페이지로 정리(뷰포트/모바일 터치 대응 포함)한다. 다음 실험 주제는 #001 프로필 결과에서 도출한다.
- 새 실험 추가 흐름이 "폴더 복사 → meta.json 수정 → push"로 끝나는지 항상 검증한다.
- 커밋은 실험 단위/기능 단위로 잘게. 잔디의 재료다.

## 10. 미확정 사항 (소유자가 결정할 것)

- [ ] 사이트 이름과 도메인 (예: `lab.____.dev`) — 현재 임시값(`site.config.json`)
- [x] 한국어/영어/병기 여부 → **병기(한국어 위주 + 영어 보조)로 확정**
- [ ] 라이선스 (코드 공개 범위)
- [ ] 실험 주기 목표 (매주 vs 격주 — 무리하지 않는 선)
